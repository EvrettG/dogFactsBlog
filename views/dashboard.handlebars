{{#if posts.length}}
<section>
  <h2>Your Posts</h2>
  <ol class="postList">
    {{#each posts as |post|}}
    <li>
      {{> dashboardInfo post}}      
    </li>
    {{/each}}
  </ol>
</section>
{{else}}
<section>
  <h3>You haven't posted anything yet</h3>
</section>
{{/if}}

<button type="submit"><a href="/dashboard/new">Add New Post</a></button>